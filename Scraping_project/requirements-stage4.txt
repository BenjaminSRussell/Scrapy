# Stage 4 Entity Summarization Requirements
# Prefer the optional extras layout from #144:
#   pip install -r requirements.txt -r requirements-ml.txt -r requirements-ocr.txt
#   # or: pip install -e ".[stage3-4]"
#
# This file remains as a convenience alias for ML extras used by Stage 4.

-r requirements-ml.txt

# Optional: GPU acceleration
# torch>=2.0.0+cu118             # PyTorch with CUDA 11.8 (for NVIDIA GPUs)

# Note: OCR/PDF extras (easyocr, pdf2image, PyPDF2, pillow) live in requirements-ocr.txt
# Note: Core deps (deltalake, pyarrow, confluent-kafka, etc.) live in requirements.txt
