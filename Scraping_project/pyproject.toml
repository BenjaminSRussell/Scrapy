# Tooling only. Full [project] packaging / optional-deps (ml, ocr, stage3-4, dev)
# are owned by PR #289 (fix #143). This PR (#144) must not ship a competing
# partial [project] table — install Stage 3/4 extras via requirements-*.txt.
[tool.black]
line-length = 120
target-version = ["py311"]
